```text
Function TENSOR_default_loop_1_Unroll1_PATH0_hiddenfunc0_5[5] 5614136919772644428 DYNAMIC_LOOP_PATH TENSOR_GRAPH {
```

| 字段                                                     | 可能含义                                      |
| ------------------------------------------------------ | ----------------------------------------- |
| `Function`                                             | 表示这是一个函数/子图 IR                            |
| `TENSOR_default_loop_1_Unroll1_PATH0_hiddenfunc0_5[5]` | 函数名，通常是编译器自动生成的                           |
| `5614136919772644428`                                  | 函数 ID / hash / 唯一标识                       |
| `DYNAMIC_LOOP_PATH`                                    | 表示这是动态 loop path 相关 IR                    |
| `TENSOR_GRAPH`                                         | 表示这是 Tensor Graph 类型的 IR                  |
| 片段                                                     | 可能含义                                      |
| `TENSOR`                                               | Tensor IR                                 |
| `default_loop_1`                                       | 默认 loop，loop 层数或 loop id 为 1              |
| `Unroll1`                                              | loop unroll 因子为 1                         |
| `PATH0`                                                | 执行路径 path 0                               |
| `hiddenfunc0`                                          | 内部隐藏函数编号 0                                |
| `[5]`                                                  | 可能是函数编号、参数数量、block id 等，需要结合具体 PyPTO 版本确认 |
```text
RAWTENSOR[  0] <3 x DT_FP32> @10"a"
RAWTENSOR[  1] <3 x DT_FP32> @12"b"
RAWTENSOR[  2] <3 x DT_FP32> @14"out"
RAWTENSOR[  3] <3 x DT_FP32> @11"INCAST_LOCAL_BUF0"
RAWTENSOR[  4] <3 x DT_FP32> @13"INCAST_LOCAL_BUF1"
RAWTENSOR[  5] <3 x DT_FP32> @15"OUTCAST_LOCAL_BUF0"

# 格式belike
# 第index个row tensor, 它的shape大小是num_shape, 类型是type, slotId, 名字
RAWTENSOR[index] <num_shape x type> @slot_id "name"
```
```
INCAST[0] <3xDT_FP32 / 3xDT_FP32> %11@10#(-1) fromSlot[13]
```
第0个输入cast, 类型是`<3xDT_FP32 / 3xDT_FP32>`, 意思可能是`<src type / dst type>`, 涉及的value是 `%11@10#(-1)`；数据来源是 `fromSlot[13]`。这里两边都是 `3 x FP32`，所以没有实际 dtype 变化。可以理解成`a->INCAST[0]`
```text
OUTCAST[  0]  <3xDT_FP32 / 3xDT_FP32> %17@14#(-1) toSlot[15]
```
可以理解成`计算结果 --> OUTCAST_LOCAL_BUF0 --> out`


# Appendix
- 内存中的IR
```
Function                        ← 一个函数(对应 .tifwkgr 整个文件)
 └── operations_: list<Operation>     ← op 列表(对应文件里一行一行的算式)
      ├── iOperands: vector<LogicalTensorPtr>   ← 该行的输入(%, %15)
      ├── oOperands: vector<LogicalTensorPtr>   ← 该行的输出(%18)
      ├── opcode: 枚举(ADD / VIEW / ASSEMBLE)   ← 决定打印成什么名字
      └── attribute: 附加参数                    ← offset 这类东西

LogicalTensor                   ← 逻辑张量(就是 %12 这种)
 ├── magic = 12                  ← %后面的编号
 ├── tensor → RawTensor          ← 指向它住在哪块 buffer(就是 @11)
 ├── shape = {3}                 ← 这段数据多大
 └── offset = {0}                ← 在 buffer 里的起始位置

RawTensor                       ← 原始 buffer(就是 @11"INCAST_LOCAL_BUF0")
 ├── rawmagic = 11 ← @后面的编号
 ├── symbol = "INCAST_LOCAL_BUF0"
 └── datatype, 声明 shape
```
